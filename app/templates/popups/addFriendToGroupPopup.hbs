<div>
	<h3>Convidar Amigo Para Grupo {{obj.pageInfo.mode.text}} </h3>
</div>
<div>
	<form class="form-group">
		<select class="form-control" id = "adversario_nome">
			{{#each obj.friends}}
				<option>{{@key}}</option>	
			{{/each}}
		</select>
		<div class="botao-wrapper">
			<button class="btn btn-default cancelar">Cancelar</button>
			<button class="btn btn-success" id="add_amigos_btn">Enviar</button>
		</div>

	</form>
</div>
